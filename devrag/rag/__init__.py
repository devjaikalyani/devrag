# RAG package: hybrid retrieval engine
#
#   from devrag.rag.service import get_pipeline   lazy singleton pipeline
#   from devrag.rag.pipeline import CodeRAGPipeline
#
# Subpackages: ingestion (loaders/chunkers), retrieval (FAISS + BM25 + rerank),
# generation (answering), evaluation (faithfulness scoring).
